apiVersion: v1
kind: Service
metadata:
  name: usermanager
  labels:
    app: usermanager
    tier: backend
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
  selector:
    app: usermanager
