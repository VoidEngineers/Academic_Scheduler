apiVersion: v1
kind: ConfigMap
metadata:
  name: mongodb-config
  labels:
    app: mongodb
data:
  # MongoDB Atlas connection string
  # For local development, uncomment the StatefulSet below
  CONNECTION_NOTE: "Using MongoDB Atlas - configure connection string in secrets"
  MONGODB_DATABASE: "academic_scheduler"
