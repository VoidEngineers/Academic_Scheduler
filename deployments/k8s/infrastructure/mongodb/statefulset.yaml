# MongoDB Atlas is being used for this application
# If you need to deploy MongoDB locally in Kubernetes, uncomment the configuration below:

# apiVersion: apps/v1
# kind: StatefulSet
# metadata:
#   name: mongodb
#   labels:
#     app: mongodb
#     tier: database
# spec:
#   serviceName: mongodb
#   replicas: 1
#   selector:
#     matchLabels:
#       app: mongodb
#   template:
#     metadata:
#       labels:
#         app: mongodb
#         tier: database
#     spec:
#       containers:
#         - name: mongodb
#           image: mongo:7
#           ports:
#             - containerPort: 27017
#               name: mongodb
#           env:
#             - name: MONGO_INITDB_ROOT_USERNAME
#               valueFrom:
#                 secretKeyRef:
#                   name: app-secrets
#                   key: MONGODB_USERNAME
#             - name: MONGO_INITDB_ROOT_PASSWORD
#               valueFrom:
#                 secretKeyRef:
#                   name: app-secrets
#                   key: MONGODB_PASSWORD
#             - name: MONGO_INITDB_DATABASE
#               value: academic_scheduler
#           resources:
#             requests:
#               memory: "512Mi"
#               cpu: "250m"
#             limits:
#               memory: "1Gi"
#               cpu: "500m"
#           volumeMounts:
#             - name: mongodb-data
#               mountPath: /data/db
#           livenessProbe:
#             exec:
#               command:
#                 - mongosh
#                 - --eval
#                 - "db.adminCommand('ping')"
#             initialDelaySeconds: 30
#             periodSeconds: 10
#           readinessProbe:
#             exec:
#               command:
#                 - mongosh
#                 - --eval
#                 - "db.adminCommand('ping')"
#             initialDelaySeconds: 5
#             periodSeconds: 5
#   volumeClaimTemplates:
#     - metadata:
#         name: mongodb-data
#       spec:
#         accessModes: ["ReadWriteOnce"]
#         storageClassName: standard
#         resources:
#           requests:
#             storage: 10Gi
