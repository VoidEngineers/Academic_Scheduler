spring.application.name=usermanagement
spring.data.mongodb.uri=mongodb+srv://it22278944:nL6kiAUrqQwBWtmC@itpm-project.k7b3m.mongodb.net/timesyncdb-dev?retryWrites=true&w=majority&appName=ITPM-PROJECT
server.port=8082

# CORS allowed origins (comma-separated list)
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# Exclude DataSource auto-configuration to avoid issues with SQL databases
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# Actuator endpoints configuration
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always
management.endpoint.prometheus.enabled=true
management.metrics.export.prometheus.enabled=true

# Service info
info.app.name=User Management Service
info.app.description=Academic Scheduler User Management
info.app.version=1.0.0
