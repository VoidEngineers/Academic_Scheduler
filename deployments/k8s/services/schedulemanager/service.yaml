apiVersion: v1
kind: Service
metadata:
  name: schedulemanager
  labels:
    app: schedulemanager
    tier: backend
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
      name: http
  selector:
    app: schedulemanager
