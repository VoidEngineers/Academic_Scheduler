apiVersion: v1
kind: Service
metadata:
  name: conflictmanager
  labels:
    app: conflictmanager
    tier: backend
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app: conflictmanager
