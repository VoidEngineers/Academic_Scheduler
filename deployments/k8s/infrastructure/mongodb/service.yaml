# Service for MongoDB (if deploying locally)
# Uncomment if using local MongoDB StatefulSet

# apiVersion: v1
# kind: Service
# metadata:
#   name: mongodb
#   labels:
#     app: mongodb
#     tier: database
# spec:
#   type: ClusterIP
#   clusterIP: None  # Headless service for StatefulSet
#   ports:
#     - port: 27017
#       targetPort: 27017
#       protocol: TCP
#       name: mongodb
#   selector:
#     app: mongodb
